# 🏪 نظام إدارة مجموعة البقالات الموحد | 2026 Edition

> **الإصدار:** 2.0.0 | **الحالة:** Production Ready ✨ | **المطور:** MAHMOUD HUSSEIN

نظام إدارة متطور وشامل لمجموعة البقالات، مصمم بأحدث التقنيات والمعايير العالمية، مع واجهة مستخدم عصرية وميزات متقدمة.

---

## 📋 جدول المحتويات

1. [المميزات](#المميزات)
2. [البنية المعمارية](#البنية-المعمارية)
3. [المتطلبات](#المتطلبات)
4. [التثبيت والتشغيل](#التثبيت-والتشغيل)
5. [الاستخدام](#الاستخدام)
6. [الصفحات والمكونات](#الصفحات-والمكونات)
7. [التكاملات](#التكاملات)
8. [الأمان](#الأمان)
9. [الترخيص](#الترخيص)

---

## ✨ المميزات

### 🎯 الميزات الأساسية

- ✅ **إدارة الفروع**: إنشاء وتعديل وحذف الفروع مع تتبع تفاصيلها
- ✅ **إدارة الموظفين**: إدارة بيانات الموظفين والرواتب والأقسام
- ✅ **إدارة المستندات**: تتبع المستندات الحكومية والصلاحيات
- ✅ **التقارير المتقدمة**: تقارير شاملة وتحليلات البيانات
- ✅ **لوحة تحكم ديناميكية**: إحصائيات فورية ورسوم بيانية تفاعلية

### 🎨 واجهة المستخدم الحديثة

- 🌈 **تصميم عصري 2026**: Glassmorphism و Gradient Effects
- 🌓 **المظهر الليلي**: تبديل سلس بين المظهر الفاتح والداكن
- 📱 **متجاوب بالكامل**: عمل مثالي على جميع الأجهزة
- ⚡ **أداء عالي**: تحميل سريع وتفاعلات فورية
- 🎭 **تأثيرات بصرية**: Animations و Transitions احترافية

### 🔐 الأمان والاستقرار

- 🔒 **تخزين آمن**: استخدام IndexedDB للبيانات المحلية
- 🛡️ **التحقق من الصحة**: معالجة أخطاء شاملة
- 📊 **تسجيل الأنشطة**: تتبع كامل للعمليات
- 🔔 **الإشعارات**: نظام تنبيهات ذكي

### 💾 إدارة البيانات

- 📈 **قاعدة بيانات قوية**: IndexedDB مع schema متقدم
- 🔄 **المزامنة التلقائية**: تحديث البيانات في الوقت الفعلي
- 📥 **الاستيراد والتصدير**: دعم Excel و PDF
- 🗂️ **تنظيم ذكي**: تصنيف وترشيح متقدم

---

## 🏗️ البنية المعمارية

### هيكل المشروع

```
ProjectControlIssues/
├── public/
│   ├── index.html              # النسخة الأساسية
│   └── index-modern.html       # النسخة الحديثة (2026)
├── src/
│   ├── services/
│   │   ├── DataManager.js      # إدارة قاعدة البيانات (IndexedDB)
│   │   ├── APIService.js       # التكاملات الخارجية
│   │   └── StateManager.js     # إدارة الحالة
│   ├── components/
│   │   ├── DataTable.js        # جدول بيانات ديناميكي
│   │   └── NotificationSystem.js # نظام الإشعارات
│   ├── js/
│   │   ├── app.js              # التطبيق الأساسي
│   │   ├── app-updated.js      # التطبيق المحدث
│   │   ├── init.js             # التهيئة الأولية
│   │   └── pages/
│   │       ├── dashboard.js    # لوحة التحكم
│   │       ├── branches.js     # إدارة الفروع
│   │       ├── employees.js    # إدارة الموظفين
│   │       ├── documents.js    # إدارة المستندات
│   │       └── reports.js      # التقارير
│   └── css/
│       ├── main.css            # الأنماط الأساسية
│       ├── layout.css          # تخطيط الصفحة
│       └── dashboard.css       # أنماط لوحة التحكم
├── index.html                  # ملف التشغيل الرئيسي
├── index.old.html              # النسخة الأصلية (نسخة احتياطية)
└── README.md                   # التوثيق

```

### نمط المعمارية

```
┌─────────────────────────────────────┐
│     User Interface (HTML/CSS)       │
│  ┌──────────────────────────────┐   │
│  │ Pages Layer (Dashboard, etc) │   │
│  └──────────┬───────────────────┘   │
└─────────────┼──────────────────────┘
              │
┌─────────────┼──────────────────────┐
│   Services Layer                    │
│  ┌──────────┴────────────────────┐  │
│  │  StateManager  APIService     │  │
│  │  DataManager   Components     │  │
│  └───────────────────────────────┘  │
└─────────────┬──────────────────────┘
              │
┌─────────────┼──────────────────────┐
│  IndexedDB / LocalStorage           │
│  Real-time Data Persistence         │
└────────────────────────────────────┘
```

---

## 📦 المتطلبات

### المتطلبات الأساسية

- متصفح حديث يدعم:
  - ES6+ JavaScript
  - IndexedDB
  - Web Components
  - CSS Grid & Flexbox

### المكتبات الخارجية

| المكتبة | الغرض | الإصدار |
|----------|-------|---------|
| Chart.js | رسوم بيانية تفاعلية | 4.4.0+ |
| XLSX | استيراد/تصدير Excel | 0.18.5+ |
| jsPDF | توليد ملفات PDF | 0.10.1+ |
| Tailwind CSS | أداة تصميم | 3.0+ |
| Font Awesome | الأيقونات | 6.4.0+ |

---

## 🚀 التثبيت والتشغيل

### 1. استنساخ المستودع

```bash
git clone https://github.com/yourusername/ProjectControlIssues.git
cd ProjectControlIssues
```

### 2. فتح في متصفح

```bash
# الطريقة 1: استخدم خادم ويب محلي
python -m http.server 8000
# ثم افتح: http://localhost:8000

# الطريقة 2: افتح الملف مباشرة
open index.html
```

### 3. التشغيل الأول

عند فتح التطبيق لأول مرة:
- سيتم إنشاء قاعدة البيانات تلقائياً
- سيتم تحميل البيانات الأولية (نموذجية)
- سيتم عرض لوحة التحكم الرئيسية

---

## 📖 الاستخدام

### لوحة التحكم (Dashboard)

عرض شامل لإحصائيات الشركة:
- إجمالي الفروع والموظفين والمستندات
- رسوم بيانية تفاعلية
- الأنشطة الأخيرة
- التنبيهات المهمة

### إدارة الفروع (Branches)

```javascript
// إضافة فرع جديد
const newBranch = {
  name: "فرع الرياض",
  organization: "مؤسسة البقالة",
  manager: "محمد أحمد",
  location: "الرياض - حي الملز",
  phone: "+966501234567",
  email: "riyadh@grocery.sa",
  active: true
};
app.dataManager.add('branches', newBranch);
```

### إدارة الموظفين (Employees)

```javascript
// إضافة موظف جديد
const newEmployee = {
  name: "فاطمة علي",
  position: "مدير مستودع",
  department: "المستودع",
  branchId: "branch_1",
  phone: "+966501234567",
  email: "fatima@grocery.sa",
  hireDate: new Date('2023-01-15').toISOString(),
  salary: 6000,
  idNumber: "1234567890",
  active: true
};
app.dataManager.add('employees', newEmployee);
```

### إدارة المستندات (Documents)

```javascript
// إضافة مستند جديد
const newDocument = {
  type: "رخصة البلدية",
  number: "BM-2024-001",
  branchId: "branch_1",
  issueDate: new Date('2024-01-01').toISOString(),
  expiryDate: new Date('2025-12-31').toISOString(),
  responsible: "محمد أحمد",
  status: "approved",
  notes: "المستند صالح ومعتمد"
};
app.dataManager.add('documents', newDocument);
```

---

## 🔧 الصفحات والمكونات

### 📊 صفحة لوحة التحكم (DashboardPage)

**الملف:** `src/js/pages/dashboard.js`

**الميزات:**
- عرض الإحصائيات الرئيسية
- رسوم بيانية تفاعلية (4 charts)
- تحديث تلقائي كل 30 ثانية
- مؤشرات الاتجاه (trend indicators)

**الرسوم البيانية:**
1. توزيع الموظفين على الفروع (Bar Chart)
2. توزيع الموظفين حسب القسم (Doughnut Chart)
3. حالة المستندات (Pie Chart)
4. النشاط الشهري (Line Chart)

### 🏢 صفحة إدارة الفروع (BranchesPage)

**الملف:** `src/js/pages/branches.js`

**الميزات:**
- جدول ديناميكي مع البيانات الكاملة
- البحث والتصفية
- إضافة/تعديل/حذف الفروع
- عرض تفاصيل الفرع
- معلومات الموظفين والمستندات

**العمليات:**
- ✏️ تعديل بيانات الفرع
- 🗑️ حذف الفرع
- 👁️ عرض التفاصيل
- ➕ إضافة فرع جديد

### 👥 صفحة إدارة الموظفين (EmployeesPage)

**الملف:** `src/js/pages/employees.js`

**الميزات:**
- جدول شامل لجميع الموظفين
- البحث والتصفية حسب القسم
- إضافة/تعديل/حذف الموظفين
- عرض تفاصيل الموظف
- حساب الرواتب والإحصائيات

**الحقول:**
- الاسم الكامل
- رقم الهوية
- الوظيفة والقسم
- الفرع
- البيانات الاتصالية
- الراتب
- تاريخ التعيين

### 📄 صفحة إدارة المستندات (DocumentsPage)

**الملف:** `src/js/pages/documents.js`

**الميزات:**
- تتبع المستندات الحكومية
- تنبيهات تلقائية عند اقتراب الانتهاء
- حالة اللون حسب الصلاحية
- البحث والتصفية
- معلومات الأيام المتبقية

**أنواع المستندات:**
- رخصة البلدية
- السجل التجاري
- السجل الضريبي
- تصريح التشغيل
- التأمينات الاجتماعية
- التصاريح الصحية

### 📊 صفحة التقارير المتقدمة (ReportsPage)

**الملف:** `src/js/pages/reports.js`

**أنواع التقارير:**
1. **ملخص الفروع**: إحصائيات شاملة عن جميع الفروع
2. **ملخص الموظفين**: بيانات الموظفين والأقسام والرواتب
3. **حالة المستندات**: تفاصيل المستندات والصلاحيات
4. **المستندات قيد الانتهاء**: المستندات منتهية أو قريبة الانتهاء
5. **تقرير الرواتب**: حساب الرواتب الإجمالية والبيانات المالية
6. **تقرير مخصص**: إنشاء تقرير حسب احتياجاتك

---

## 🔌 التكاملات

### Services Layer

#### 1. DataManager (إدارة البيانات)

```javascript
// إنشاء سجل جديد
await app.dataManager.add('branches', data);

// الحصول على سجل معين
const branch = await app.dataManager.get('branches', branchId);

// الحصول على جميع السجلات
const branches = await app.dataManager.getAll('branches');

// البحث المتقدم
const results = await app.dataManager.query('employees', 'branchId', branchId);

// التحديث
await app.dataManager.update('branches', updatedData);

// الحذف
await app.dataManager.delete('branches', branchId);
```

#### 2. APIService (التكاملات الخارجية)

```javascript
// الحصول على حالة المستند الحكومي
const status = await app.apiService.getGovDocStatus(docNumber);

// إرسال بريد إلكتروني
await app.apiService.sendEmail({
  to: 'user@example.com',
  subject: 'الموضوع',
  body: 'محتوى الرسالة'
});

// إرسال رسالة نصية
await app.apiService.sendSMS(phoneNumber, message);

// توليد ملف PDF
const pdfBlob = await app.apiService.generatePDF(data);

// توليد ملف Excel
const excelBlob = await app.apiService.generateExcel(data);
```

#### 3. StateManager (إدارة الحالة)

```javascript
// تعيين قيمة في الحالة
app.stateManager.setState('theme', 'dark');

// الاستماع للتغييرات
app.stateManager.subscribe('theme', (newTheme) => {
  console.log(`المظهر تغير إلى: ${newTheme}`);
});

// إضافة إشعار
app.stateManager.addNotification('تم الحفظ بنجاح', 'success');

// تبديل المظهر
app.stateManager.toggleTheme();
```

### Web Components

#### DataTable Component

```html
<data-table 
  id="myTable"
  columns="name,email,status"
  searchable="true"
  sortable="true"
>
</data-table>

<script>
// تعيين البيانات
document.getElementById('myTable').setData(data, [
  { field: 'name', label: 'الاسم' },
  { field: 'email', label: 'البريد' },
  { field: 'status', label: 'الحالة' }
]);
</script>
```

#### NotificationSystem Component

```javascript
// إنشاء إشعار
app.notify('تم الحفظ بنجاح', 'success');

// الأنواع المختلفة
app.notify(message, 'success');   // أخضر
app.notify(message, 'error');     // أحمر
app.notify(message, 'warning');   // أصفر
app.notify(message, 'info');      // أزرق
```

---

## 🔐 الأمان

### معايير الأمان المطبقة

1. **التحقق من الصحة**: جميع المدخلات يتم التحقق منها
2. **التشفير المحلي**: البيانات المحلية محمية
3. **عدم التعرض للـ API**: لا توجد مفاتيح API في الكود
4. **معالجة الأخطاء**: معالجة شاملة للاستثناءات
5. **تسجيل الأنشطة**: تتبع كامل للعمليات

### نصائح الأمان

```javascript
// استخدم دائماً التحقق
if (!validateInput(userData)) {
  throw new Error('بيانات غير صحيحة');
}

// تجنب تخزين البيانات الحساسة محلياً
// استخدم sessionStorage للبيانات المؤقتة

// تحقق من الأذونات قبل تنفيذ العمليات
if (!user.permissions.includes('edit_documents')) {
  throw new Error('ليس لديك صلاحية');
}
```

---

## 📱 الأجهزة المدعومة

| الجهاز | المتصفح | الدعم |
|-------|--------|-------|
| الويب | Chrome | ✅ كامل |
| الويب | Firefox | ✅ كامل |
| الويب | Safari | ✅ كامل |
| الويب | Edge | ✅ كامل |
| الهاتف | Chrome Mobile | ✅ كامل |
| الجهاز اللوحي | Safari iPad | ✅ كامل |

---

## 📊 إحصائيات المشروع

| البند | القيمة |
|------|--------|
| عدد الملفات | 15+ |
| أسطر الكود | 4000+ |
| عدد الصفحات | 5 |
| عدد الخدمات | 3 |
| عدد المكونات | 2+ |
| عدد التقارير | 6 |

---

## 🐛 استكشاف الأخطاء

### المشكلة: قاعدة البيانات فارغة

```javascript
// الحل: أعد تحميل البيانات
await app.initializeSampleData();
```

### المشكلة: الرسوم البيانية لا تظهر

```javascript
// تأكد من وجود مكتبة Chart.js
// <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
```

### المشكلة: الإشعارات لا تظهر

```javascript
// تحقق من أن الحاوية موجودة
console.log(document.getElementById('notificationContainer'));
```

---

## 📚 موارد إضافية

- [Chart.js Documentation](https://www.chartjs.org/)
- [IndexedDB API](https://developer.mozilla.org/en-US/docs/Web/API/IndexedDB_API)
- [Web Components](https://developer.mozilla.org/en-US/docs/Web/Web_Components)
- [CSS Grid](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Grid_Layout)

---

## 🎯 خارطة الطريق

### النسخة 2.1 (قريباً)
- ✅ تكاملات حقيقية مع الحكومة السعودية
- ✅ نظام المستخدمين والأذونات
- ✅ دعم النسخ الاحتياطية التلقائية

### النسخة 2.2 (قريباً)
- ✅ تطبيق Mobile Native
- ✅ دعم Offline Mode
- ✅ نظام الإشعارات المتقدم

### النسخة 3.0 (المستقبل)
- ✅ Backend API كامل
- ✅ دعم Multi-branch الفعلي
- ✅ نظام الذكاء الاصطناعي للتنبؤات

---

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT. انظر [LICENSE](LICENSE) للتفاصيل.

---

## 👨‍💻 المطور

**MAHMOUD HUSSEIN**

- 📧 البريد الإلكتروني: mahmoud@example.com
- 🌐 الموقع: [portfolio.example.com](https://portfolio.example.com)
- 💼 LinkedIn: [mahmoud-hussein](https://linkedin.com)

---

## 🙏 شكر وتقدير

شكر خاص للمساهمين وجميع من ساعدوا في تطوير هذا المشروع.

---

**آخر تحديث:** 2024-12-19
**الإصدار الحالي:** 2.0.0
**الحالة:** 🟢 Production Ready
